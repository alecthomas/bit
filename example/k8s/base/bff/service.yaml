apiVersion: v1
kind: Service
metadata:
  name: bff
  namespace: example
  labels:
    app: bff
spec:
  type: NodePort
  selector:
    app: bff
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30000
