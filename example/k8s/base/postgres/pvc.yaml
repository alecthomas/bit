apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: example
  labels:
    app: postgres
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
